= link_to "< Back", :back, class: "tag_link"
%h2 Edit this tag...

= semantic_form_for @tag, url: dispatch_tag_path(@tag.id) do |f|
  = f.input :name
  = f.input :group, as: :select, collection: @groups
  = f.actions
-# = form_for(@tag, url: dispatch_tag_path(@tag.id)) do |f|
-#   %h2
-#     = f.label :name, "Tag name:"

-#   = f.text_field(:name)
-#   = f.select :group_id, @groups.collect{|g| [g.name, g.id] }
-#   = f.submit "Submit it"

