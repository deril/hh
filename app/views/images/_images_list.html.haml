%article{ class: "image_list_wrap" }
  - if @cur_tag.presence
    %h2= "Current tag: #{@cur_tag.name.capitalize}"
    
  - if imgs.present?
    %ul
      - imgs.each do |img|
        %li= link_to image_tag(img.image.url(:thumb)), image_path(img.id), class: "image_thumbs"
  - else
    No images present...

  = paginate imgs