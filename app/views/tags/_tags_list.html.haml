- all_tags = true if local_assigns[:all_tags]
%aside
  %h2 Tags:
  - if tags.presence
    - tags.each do |tag|
      = link_to "#{tag.name} (#{tag.count})", tag_path(tag.id), class: "tag_link block", alt: "#{tag.name}"
    - if all_tags  
      = link_to "All tags",tags_path(), { class: "tag_link block special" }
  - else
    No tags present...