- warn_id = local_assigns[:selected_warn_id]
%h2 Warns:
- if warns.presence
  - warns.each do |warn|
    = link_to warn.name, warn_path(warn), class: "tag_link block#{" active" if warn_id == warn.id}", title: warn.name
- else
  Nothing to show
